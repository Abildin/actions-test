name: Auto approve

on: pull_request

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: Abildin/auto-approve-action@v1.0.0
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        configs: '.github/configs/auto-approve.json'